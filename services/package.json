{
  "name": "heirloom-services",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Heirloom off-chain layer: heartbeat listener/indexer, Telegram /alive bot, email claim links. Testnet only, unaudited.",
  "engines": { "node": ">=22" },
  "scripts": {
    "build": "tsc --noEmit",
    "start": "tsx src/server.ts",
    "indexer": "tsx src/indexer.ts",
    "test": "tsx --test test/*.test.ts",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "viem": "^2.21.0"
  },
  "devDependencies": {
    "@types/node": "^22.9.0",
    "tsx": "^4.19.0",
    "typescript": "^5.6.0"
  }
}
